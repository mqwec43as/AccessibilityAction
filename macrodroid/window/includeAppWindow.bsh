/*
 * Include all application windows WindowInfo.TYPE_APPLICATION
 */
includeAppWindow() {
    if (WindowInfo == void) WindowInfo = WindowInfo();
    return addWindowFilter(WindowInfo.TYPE_APPLICATION);
}