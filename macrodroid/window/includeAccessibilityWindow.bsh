/*
 * Include all accessibility windows WindowInfo.TYPE_ACCESSIBILITY_OVERLAY
 */
includeAccessibilityWindow() {
    if (WindowInfo == void) WindowInfo = WindowInfo();
    return addWindowFilter(WindowInfo.TYPE_ACCESSIBILITY_OVERLAY);
}
