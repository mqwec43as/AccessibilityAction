/*
 * Exclude all application windows WindowInfo.TYPE_APPLICATION
 */
excludeAppWindow() {
    if (WindowInfo == void) WindowInfo = WindowInfo();
    return removeWindowFilter(WindowInfo.TYPE_APPLICATION);
}